package common.support;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public abstract class AbstractTriggerSupport {

	/** 记录日志 */
	protected Log log = LogFactory.getLog(AbstractTriggerSupport.class.getName());

}
